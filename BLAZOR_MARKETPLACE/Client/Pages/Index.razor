@page "/"

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

<MudDatePicker Color="Color.Info" Class="form-control" @bind-Date="date" Editable="true" Mask="@(new DateMask("MM/dd/yyyy"))" DateFormat="MM/dd/yyyy" Placeholder="00/00/0000" />

<h3>@data</h3>
<div class="col-6">
    <MudDatePicker Color="Color.Info" Class="form-control" @bind-Date="date" Editable="true" Mask="@(new DateMask("MM/dd/yyyy"))" DateFormat="MM/dd/yyyy" Placeholder="00/00/0000" />
</div>

@code{
    public DateTime? date{ get; set; }
    private string data { get; set; } = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        DateTime dataOut;
        string time = "19:00";
        if (DateTime.TryParse(time, out dataOut))
        {
            var x = DateTime.Parse(time);
            data = dataOut.ToString();            
        }
    }
}